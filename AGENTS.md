# 项目美术与页面文案规则

本项目绘制或修改任何素材时，都必须遵循已经敲定的美术风格。此规则覆盖独立物件、图标及其状态变化，也适用于生成素材和直接编写 SVG；用户后续明确批准的局部调整应记录在对应需求中。

所有涉及美术素材重绘或新增的改动，都必须先向用户展示具体预览，并取得用户对该版本的明确同意，才能接入线上发布。常规提交与推送授权不能代替素材审批。待确认素材与对照预览放在 `codex-generated` 中，不得混入可自动部署的正式素材或页面；经同意后再接入正式路径并发布。此规则同样适用于以 SVG、CSS、Canvas 或生成图片制作的新素材，不能通过更换实现方式跳过预览与确认。

制作前阅读[教室现行美术参照](docs/classroom-art-reference.md)，以用户最新确认并正式接入的页面、素材和样式为准，同时对照当前已确认的相邻物件。2026-09-09 的早期效果图与生成提示词只用于追溯来源，不再作为后续制作必须逐项一致的固定模板；未批准的中间稿也不能替代成品参照。沿用低饱和配色、简化轮廓和少量平面色块，以哑光质感表达物件；新增素材不得采用写实产品渲染、密集高光或复杂体积明暗。官方实物图片只用于核对外形与比例，不能替代项目美术参照。已经明确批准的纹理、灯光效果和局部配色继续保留。

物件放置只允许等比缩放、平移或旋转，并须检查与相邻物件的大小比例。同一桌面上的物件必须使用一致的观看角度，接触桌面的底缘也须对应场景位置，不能呈现悬空或翘起的错误姿态。完成后将新素材与实际相邻素材放在一起核对，禁止以风格已偏离的中间版本作为新的标准，也不得因局部修改改变其他已确认物件。

在软件或网站开发时，禁止自行添加任何说明文字。只能添加必要的按钮文字，或在用户明确指引下添加说明文字。

需求表中的验收待办仅保留需要远端成员或其设备配合的项目，本地页面观感和常规使用效果不单独设验收待办，由用户在使用中反馈问题。代码修改仍执行必要的构建与相关检查；取消验收待办不得记为验收通过，未实现或暂缓的功能也不得因此移除。当前待办以 `docs/remaining-priority-requirements.md` 为准，不从历史验证记录中恢复已取消的验收项。

当前功能统一维护在 `docs/current-functionality.md`，首次完整说明之后的改动持续记录在同目录的 `docs/functionality-changelog.md`。功能规则变化时，在同一工作单元对照代码更新现行说明，并向日志追加实际变化，区分已实现行为和未验证边界；日志起点固定为首次完整说明提交 `5714334`，旧规则被替代时注明后续调整。Git 提交继续保留简短说明，不再按每次改动新增其他专题实施日志。历史资料通过 `docs/README.md` 与 `docs/archive/README.md` 查阅，只保留有独立维护价值的来源、批准范围和兼容依据；重复或失效的逐次记录可合并、归档或删除。整理时修复引用，不从历史记录恢复已经取消的待办，素材署名及许可文件随素材保留。

功能变更日志按日期分组，每项改动使用独立小节和直接说明变化的标题，正文用简短段落或少量要点展开。行为变化与后续替代关系分别说明，保留对应提交链接，避免使用承载大量正文的长表格或把多个变化集中写在同一段。
